export class MemoryManager {
    readByte(address : number) { // Read 8 bit from address
        return 0;
    }
    readWord(address : number) { // Read 16 bit from address
        return 0;
    }

    writeByte(address : number, value : number) { // Write 8 bit to address

    }
    writeWord(address : number, value : number) { // Write 16 bit to address

    }
}